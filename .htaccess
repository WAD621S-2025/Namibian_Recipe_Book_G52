<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /Namibian_recipe_Book/

    # Redirect all requests except existing files/folders to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?page=$1 [L,QSA]
</IfModule>
